# Packages

| Package | Purpose | Status |
| --- | --- | --- |
| [`@inttegro/mcp-doctor`](mcp-doctor) | Read-only OAuth, protection, initialization, and tool-catalog diagnostics | Source release |

Reusable AI libraries and protocol adapters belong here when they are useful
independently of a host plugin. Each package defines its supported runtime,
public API, release process, and compatibility policy.
