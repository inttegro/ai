# Packages

Reusable AI libraries and protocol adapters belong here when they are useful independently of a host plugin. Each package should define its supported runtime, public API, release process, and compatibility policy.
